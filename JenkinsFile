try {
	node (label:'selenium'){
		stage ('Setup') {
			helper = load "${env.WORKSPACE}/src/helpers.groovy"
		}
		
		stage ('Project Checkout'){		
			heler.info("Yay"
			
			 
		}
	}		
	
} catch(Exception ex) {
	println(ex)
	
    currentBuild.result = 'FAILURE'
} finally {
	echo "===Done  ==============="
}