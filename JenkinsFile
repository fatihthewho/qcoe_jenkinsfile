try {
	node (label:'selenium'){
		stage ('Setup') {
		    	echo "===accessing Team's repo ${env.WORKSPACE}"
		echo "===accessing Team's repo ${env.WORKSPACE.split('@')[0]} ==============="
		}
		stage ('Project Checkout'){		
			echo "===accessing Team's repo  ==============="
			
			 
		}
	}		
	
} catch(Exception ex) {
	println(ex)
	
    currentBuild.result = 'FAILURE'
} finally {
	echo "===Done  ==============="
}