try {
	node (label:'selenium'){
		stage ('Setup') {
			helper = load "${env.WORKSPACE.split('@')[0]}@script/src/helpers.groovy"
		}
		
		stage ('Project Checkout'){		
			echo "===accessing Team's repo  ==============="
			
			 
		}
	}		
	
} catch(Exception ex) {
	println(ex)
	
    currentBuild.result = 'FAILURE'
} finally {
	echo "===Done  ==============="
}